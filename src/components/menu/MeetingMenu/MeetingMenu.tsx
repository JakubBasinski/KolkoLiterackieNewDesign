import React from 'react';
import styles from './meetings.module.scss';

interface MeetingProps {
    className?: string;
    classes: string | null;
}

export const MeetingMenu = ({ className, classes }: MeetingProps) => {
    return <div>MeetingMenu</div>;
};
