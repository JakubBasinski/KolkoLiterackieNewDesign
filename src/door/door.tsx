import React from 'react';
import { motion } from 'framer-motion';
import classNames from 'classnames';
import styles from './door.module.scss';

export interface DoorProps {
    className?: string;
}

export const Doors = ({ className }: DoorProps) => {
    return <></>;
};
